import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lsavic.FindMyWeather',
  appName: 'FindMyWeather',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
